Spring Security

  Spring Security is a powerful, highly customizable authentication and access-control framework for Java/Spring applications. Here's a structured
  breakdown:

  ---
  Core Concepts

  1. Authentication vs Authorization

  - Authentication — Who are you? (verifying identity via username/password, tokens, etc.)
  - Authorization — What are you allowed to do? (checking roles/permissions after login)

  ---
  2. The Security Filter Chain

  Spring Security works as a chain of servlet filters that intercept every HTTP request before it reaches your controllers.

  HTTP Request
      ↓
  [Filter 1: UsernamePasswordAuthenticationFilter]
      ↓
  [Filter 2: BasicAuthenticationFilter]
      ↓
  [Filter 3: ExceptionTranslationFilter]
      ↓
  [Filter 4: FilterSecurityInterceptor]
      ↓
  Your Controller

  Each filter has a specific responsibility. You configure which filters run and in what order via SecurityFilterChain.

  ---
  3. Key Components

  ┌────────────────────────┬──────────────────────────────────────────────┐
  │       Component        │                     Role                     │
  ├────────────────────────┼──────────────────────────────────────────────┤
  │ SecurityFilterChain    │ Defines which requests are secured and how   │
  ├────────────────────────┼──────────────────────────────────────────────┤
  │ AuthenticationManager  │ Delegates authentication to providers        │
  ├────────────────────────┼──────────────────────────────────────────────┤
  │ AuthenticationProvider │ Actually validates credentials               │
  ├────────────────────────┼──────────────────────────────────────────────┤
  │ UserDetailsService     │ Loads user data from DB/memory               │
  ├────────────────────────┼──────────────────────────────────────────────┤
  │ UserDetails            │ Represents the authenticated user            │
  ├────────────────────────┼──────────────────────────────────────────────┤
  │ PasswordEncoder        │ Hashes/verifies passwords (e.g., BCrypt)     │
  ├────────────────────────┼──────────────────────────────────────────────┤
  │ SecurityContext        │ Holds the current user's authentication info │
  └────────────────────────┴──────────────────────────────────────────────┘

  ---
  4. How Authentication Flow Works

  User submits login form
          ↓
  UsernamePasswordAuthenticationFilter intercepts
          ↓
  Creates Authentication token (unauthenticated)
          ↓
  Passes to AuthenticationManager
          ↓
  Delegates to AuthenticationProvider
          ↓
  Calls UserDetailsService.loadUserByUsername()
          ↓
  Compares password with PasswordEncoder
          ↓
  Returns authenticated token → stored in SecurityContext

  ---
  5. Common Configurations

  Securing endpoints:
  http
    .authorizeHttpRequests(auth -> auth
        .requestMatchers("/admin/**").hasRole("ADMIN")
        .requestMatchers("/public/**").permitAll()
        .anyRequest().authenticated()
    );

  Login/logout:
  http
    .formLogin(form -> form.loginPage("/login").permitAll())
    .logout(logout -> logout.logoutUrl("/logout"));

  Password encoding:
  @Bean
  PasswordEncoder passwordEncoder() {
      return new BCryptPasswordEncoder();
  }

  ---
  6. Authorization Methods

  - URL-based — configured in SecurityFilterChain (as above)
  - Method-level — using annotations on service/controller methods:
  @PreAuthorize("hasRole('ADMIN')")
  public void deleteUser(Long id) { ... }

  @Secured("ROLE_ADMIN")
  public void anotherMethod() { ... }

  ---
  7. Common Security Mechanisms

  ┌────────────────────┬──────────────────────────────────────────────────────────────────┐
  │     Mechanism      │                           Description                            │
  ├────────────────────┼──────────────────────────────────────────────────────────────────┤
  │ Form Login         │ Traditional HTML login form                                      │
  ├────────────────────┼──────────────────────────────────────────────────────────────────┤
  │ HTTP Basic         │ Username/password in request header                              │
  ├────────────────────┼──────────────────────────────────────────────────────────────────┤
  │ JWT                │ Stateless token-based auth (common for REST APIs)                │
  ├────────────────────┼──────────────────────────────────────────────────────────────────┤
  │ OAuth2/OpenID      │ Third-party login (Google, GitHub, etc.)                         │
  ├────────────────────┼──────────────────────────────────────────────────────────────────┤
  │ CSRF protection    │ Enabled by default for browser apps, disabled for stateless APIs │
  ├────────────────────┼──────────────────────────────────────────────────────────────────┤
  │ Session Management │ Controls how sessions are created/invalidated                    │
  └────────────────────┴──────────────────────────────────────────────────────────────────┘

  ---
  8. CSRF (Cross-Site Request Forgery)

  - Enabled by default for form-based apps
  - For REST APIs (stateless), you typically disable it:
  http.csrf(csrf -> csrf.disable());

  ---
  9. The SecurityContext

  After authentication, the user's identity is stored in SecurityContextHolder:

  Authentication auth = SecurityContextHolder.getContext().getAuthentication();
  String username = auth.getName();
  Collection<? extends GrantedAuthority> roles = auth.getAuthorities();

  By default it uses a ThreadLocal strategy — each request thread has its own security context.

  ---
  Big Picture Summary

  Request → Filter Chain → Authentication → SecurityContext
                                ↓
                      Authorization checks (URL or method-level)
                                ↓
                       Access Granted / 403 Forbidden

  Spring Security is highly extensible — you can plug in custom UserDetailsService, AuthenticationProvider, filters, etc. to fit any auth strategy.

To customize security, create a SecurityConfig class that extends WebSecurityConfigurerAdapter and use the @EnableWebSecurity annotation.

Key Configuration Methods:
authorizeRequests(): Defines which URLs are public and which are restricted.

Example: Allow /public/** but require authentication for all other requests.

formLogin(): Enables the standard login form.

.loginPage("/login"): Specifies the custom login page URL.

.permitAll(): Ensures everyone can access the login page.

logout(): Configures secure sign-out functionality.

4. Creating the Login Form
Create a login.html file in src/main/resources/templates. The form must meet these requirements for Spring Security to process it:

Method: Must be POST.

Action: Should point to /login.

Fields: Include input fields for username and password.

Submit: A button to trigger the authentication request.

5. Implementing Password Encryption
Storing passwords in plain text is a major security risk. Spring Security uses the PasswordEncoder interface to handle hashing.

The 5-Step Process:
Add Dependency: Ensure spring-boot-starter-security is in your pom.xml.

Configure Bean: Define a BCryptPasswordEncoder bean in your security config. BCrypt is a strong hashing algorithm that adapts to hardware improvements over time.

Encrypt on Save: In your User Service, use passwordEncoder.encode(rawPassword) before saving the user to the database.

Define User Entity: Create a Java class with JPA annotations (@Entity) to map to your database, including id, username, and password fields.

Test: Verify that when a user registers, the database stores a hashed string rather than the actual password.

Summary Table: Key Components
Component	Responsibility
WebSecurityConfigurerAdapter	The base class for customizing security settings.
BCryptPasswordEncoder	The recommended implementation for secure password hashing.
form-login	The mechanism that redirects unauthorized users to a login page.
UserDetailsService	(Optional) Used to fetch user data from a custom source/database.


Role-based access control (RBAC)
RBAC regulates access to computer or network resources based on the roles of individual users within an organization. Organizations widely adopt this approach because it simplifies permission management. Instead of assigning permissions to each user, administrators assign users to roles with specific permissions.

Core concepts of RBAC
Roles
Organizations define roles according to job functions within their structure. Each role contains a set of permissions associated with it. For example, a "Manager" role may include permission to approve leave requests, while an "Employee" role may not.

Permissions
Permissions represent the right to perform certain operations on resources. Administrators assign permissions to roles rather than individual users, streamlining the process of managing who can do what within the system.

Users
Users are individuals who access the system. Organizations assign roles to users based on their responsibilities and job functions.

Sessions
A session refers to a user's interaction with the system at a particular time. During a session, a user can activate one or more roles, gaining the permissions associated with those roles.

Advantages of implementing RBAC
Simplified management
Organizations more easily manage and audit access rights by grouping permissions into roles. When a new employee joins the company, administrators assign them a role, automatically providing all necessary permissions.

Increased security
RBAC ensures users have only the access they need to perform their job functions, reducing the risk of unauthorized access to sensitive information.

Scalability
As organizations grow, managing access for numerous users becomes more complex. RBAC provides a scalable solution that easily adapts to organizational structure or personnel changes.

Compliance and audit
RBAC helps organizations comply with regulatory requirements by clearly documenting who has access to specific resources and why.

Spring Security's RBAC capabilities
Spring Security provides a powerful and customizable authentication and access control framework for Java applications. It offers comprehensive support for implementing RBAC in applications.

Key features of Spring Security for RBAC
Role-based authorization
Spring Security allows developers to define roles and assign permissions within applications. You can specify which roles can access specific resources or actions, ensuring only authorized users perform sensitive operations.

Flexible configuration
With Spring Security, developers can configure RBAC using annotations, XML configuration files, or programmatically in code. This flexibility allows the implementation of security policies that best fit application needs.

Integration with existing systems
Spring Security integrates with various authentication providers, such as LDAP, OAuth2, and OpenID Connect. This makes managing user roles and permissions across different systems within an organization easier.

Customizable access control
Spring Security provides hooks for implementing custom access control logic. This means developers can define complex rules for determining who can access what resources based on unique organizational requirements.

Session management
Spring Security offers robust session management features, including session timeout and concurrent session control. This ensures secure user sessions and protects sensitive data from unauthorized access.

Implementing RBAC with Spring Security
To implement RBAC using Spring Security, developers typically start by defining roles and permissions within the application context. They then use Spring Security's configuration options to specify which roles have access to which resources. This might involve setting up URL-based security restrictions, method-level security annotations, or custom access decision logic.

Summary
RBAC simplifies security management by assigning permissions to roles rather than individual users
The four key components of RBAC include roles, permissions, users, and sessions
RBAC provides numerous benefits, including simplified management, enhanced security, scalability, and improved compliance
Spring Security offers robust RBAC implementation for Java applications through role-based authorization, flexible configuration options, and integration capabilities
Spring Security enables customizable access control and comprehensive session management
Implementing RBAC with Spring Security involves defining roles and permissions, then configuring access rules for resources

Link to notes : https://author-ide.skills.network/render?token=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJtZF9pbnN0cnVjdGlvbnNfdXJsIjoiaHR0cHM6Ly9jZi1jb3Vyc2VzLWRhdGEuczMudXMuY2xvdWQtb2JqZWN0LXN0b3JhZ2UuYXBwZG9tYWluLmNsb3VkL0ItNXFNak1LQ2ZBWHJfMDNsSEJWbFEvQ29kaW5nJTIwQ0hlYXRzaGVldCUyME1lZHVsZSUyMDMtdjEubWQ_dD0xNzUxNTQ0OTU0IiwidG9vbF90eXBlIjoidGhlaWEiLCJhdGxhc19maWxlX2lkIjoyOTU0MTcsImFkbWluIjpmYWxzZSwiaWF0IjoxNzUxNTQ0OTU1fQ.GLwW3Hzo8ovrn27o40ztT3vO08za3axEio0s17IOMjc

Link to notes : https://author-ide.skills.network/render?token=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..WDybOVFss-DlLr9-mvaMGBRa1ohONMoYU00WgR-NpgA

Link to notes : https://author-ide.skills.network/render?token=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..Uwlw4NQ66E2SvSPcVLCpj4d5rVK5q2o1wAYS9LGBNvw

Link to notes : https://author-ide.skills.network/render?token=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..5RoH7HVULf1V_ORUiDls4-li7fTQUWw7Ajo3ndOAEF4

Link to notes : https://author-ide.skills.network/render?token=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJtZF9pbnN0cnVjdGlvbnNfdXJsIjoiaHR0cHM6Ly9jZi1jb3Vyc2VzLWRhdGEuczMudXMuY2xvdWQtb2JqZWN0LXN0b3JhZ2UuYXBwZG9tYWluLmNsb3VkL0lTcHlCRms3X3Vtd0FzZnA3blhjdVEvQ3JlYXRlJTIwYSUyMFNwcmluZyUyME1WQyUyMFByb2plY3QtdjEubWQ_dD0xNzUxNTQ2MTUwIiwidG9vbF90eXBlIjoidGhlaWEiLCJhdGxhc19maWxlX2lkIjoyOTI5MjgsImFkbWluIjpmYWxzZSwiaWF0IjoxNzUxNTQ2MTUwfQ.7jXjxeT6LBvtLvffbU06VNYuy8xxh19pkFqQuROMLH4

